export default function Home() {
  return (
    <div className="absolute top-4 left-4 z-10 pointer-events-none">
      {/* 
         Map overlay intentionally left blank for now.
         The Map background is rendered globally via AppShell.
       */}
    </div>
  );
}
